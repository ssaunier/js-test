var assert = require('assert');
var file = require('../lib/file').file;

describe('Array', function() {
  describe('#indexOf()', function () {
    it('should return null', function() {
      assert.equal(null, file());
    });
  });
});
